# 2026-09-10 公共订阅本地验收

公共 Feed 路径补齐 T12/T25，QA-15 的页面迁移提示已实现。全部证据为本机运行或隔离测试；没有创建云资源、订阅外部日历、推送 Git 或部署。

## 自动验证

- 后端92项 pytest通过，ruff通过，2项既有 Starlette/httpx/anyio弃用警告；9项新增测试在 `tests/test_public_feeds.py`。
- 覆盖：匿名GET/HEAD/304不写库、不创建账号；公共与私人路径/内容隔离；未审核公共链接不发布；同场公共/私人UID独立；改期、日期待定、取消、无变化重建；发布/撤回直播；个人屏蔽不改变公共日历；发布失败回滚与恢复；分发配置和演示限制；空状态、日调度、窗口撤销记录；Provider事务回滚和改期后的同UID更新。
- Web类型、扩展类型、production构建通过；OpenAPI与生成的前端类型同步。类型检查发现 `.next/types` 下4份带“ 2”的重复生成文件，逐项确认与原文件字节相同后仅删除重复副本；未改源码检查条件。

## 数据库与真实HTTP

新增 `7229fa56d28e` 迁移先经过隔离有数据SQLite upgrade/check/downgrade/upgrade/check，MySQL仅验证离线DDL。主库备份 `data/before-public-feeds-20260910-012243.db`（0600），升级后原22张表逐行相同、163条赛程保留，alembic check无差异。

重启主API/worker后发布14份公共来源日历，后台待处理公共任务读回为0。匿名HTTP实际解析：演示湖人12条、已有Jolpica F1样本在90/180天窗口内85个分场次；两者条件GET为304。此轮没有重新获取上游，不把本地F1条数视为新一轮官方赛历核对。公共发布和浏览器操作结束后，users/feeds/events/sources/sessions/links六表与备份相同，原12条个人投影也逐行相同。

## 浏览器

在 `http://localhost:3000/subscription` 使用未登录游客状态操作；没有退出用户原先的127.0.0.1体验会话。

选择演示湖人后，复制按钮显示成功。工具的虚拟剪贴板读回为空，因此没有用该结果声称复制成功；随后将真实快捷键粘贴到临时关注搜索框，读回完整公共地址一致。关闭临时搜索页，没有提交关注。Tab从地址框到复制按钮、Enter复制通过。

实际点击下载产生浏览器download事件。下载文件 `/Users/shizhiqi/Downloads/anke-sports-public.ics` 为6474字节、12条演示事件；SHA256为 `bb2d23a23b5799c710f09adf240349b1e8be0e2cac1a6f840cd31bc9c35fb756`，与已发布HTTP快照相同。

切换真实赛程后显示F1的85条记录及本机公开地址。1440×1000、1280×800、1024×800实际DOM宽度读回匹配，无横向溢出；检查页控制台无error。订阅页面保留为标签12，复制测试标签13已关闭，viewport恢复默认；原日历与合成直播维护页保留。

## 未完成

T12/T25继续in_progress。QA-15已具备重复风险提示和独立UID的本地证据，真实同时订阅与人工迁移仍未执行。还需HTTPS服务、真实日历刷新、账号/播放验证、来源分发许可、Azure/MySQL与容量验收。完整产品目标仍继续。
