# 文档日历路径验收 · 2026-09-10

结果：独立文档 API、持久 outbox/本地 Queue、另一个 worker 进程和既有桌面客户端已连通。没有创建 Azure 资源、部署、迁移原库或修改其他 Anke 产品。

- 最终全量后端：**277 passed、2 skipped、2条已有弃用警告**，12.89秒。新文档 HTTP 用例6项、SDK Change Feed 用例1项；此前新用例与基础用例的专项21项通过，不能与全量相加。
- Ruff 与 `git diff --check` 通过；完整 OpenAPI JSON 与已有合同逐项相同。客户端本批只有 README/STATE 变化，未重复构建。
- HTTP 覆盖：游客权限、本地登录、来源/赛程、稳定分页、失效游标、关注预览、旧预览拒绝、配置冲突、发布后重放原幂等响应、Feed 暂停/恢复、令牌轮换、改期保持 UID、SEQUENCE 仅变更事件增加、GET/HEAD/304、退出、未迁移功能明确失败。
- 权威赛程覆盖：完整指针发布前中断，旧快照保持；空输入不清空旧赛事；稳定来源身份与跨 Provider 冲突；旧版本写入拒绝；不可变快照与当前版本校验。
- 投递覆盖：Queue 发送响应丢失后断点不前移、重复投递安全、完成后空闲、断点写入不进入观察源、退避时间持久化、独立连接继续执行、进程启动不加载 `app.db` / `app.sql_app`。
- 真实 Cosmos SDK 使用完全离线 HttpTransport，模拟两个物理分区、ETag/304和分页；跨范围续读和空页断点保持通过。没有 Azure 请求或真实 RU。初版传输夹具缺少分区目录 ETag/304 而持续枚举，已修正；随后发现并处理 SDK 初始空 token 问题。调试进程均已结束，最终正常回归通过。

浏览器实际在 `http://localhost:3006/following` 进入本地体验、选择演示联赛、查看12条新增（含2条历史）、确认保存，再到月历读回12场“已加入个人日历”。截图检查了实际渲染；没有新增 UI 设计或声称完成新一轮多尺寸验收。页面 error 日志为空。

另一个 HTTP 客户端只读回浏览器所用临时体验账号结果：账号revision=1、Feed revision=2、已发布12条唯一UID，条件304、HEAD空正文通过；它自己的临时登录已退出，浏览器会话保留。不会把“日历勾选”代替实际 Feed 发布证据。机器结果见 [JSON](document-runtime-2026-09-10.json)，不含地址令牌或正文。

可检查实例：API/HTML proxy session67857、PID83337；独立 worker PID83338；IAB标签28已保留在日历。全部为合成数据，停止实验会清理临时库。首次尝试3005时端口已占用，自动清理了本次临时进程后改用3006，没有停止原3005服务。主预览/Firebase实例未重启，本批未重新验证其功能。

边界：本批没有真实 Firebase 身份调用、Cosmos 数据面、Azure Queue/Timer、真实 Provider 更新或手机日历/播放验证。YouTube、公共 Feed、个人内容、OAuth/MCP、删除、完整迁移、GC和反向关注索引仍需完成。现有 SQL 功能保留，不把文档接口子集列为整产品完成。
