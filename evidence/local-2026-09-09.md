# 本地证据 · 2026-09-09

产品：Anke Sports。范围：本机 SQLite/API/worker/浏览器。无 production、staging、手机或真实 Firebase 结论。

- `uv run pytest -q`：33 passed，0 failed；2 项第三方库弃用提醒。
- 涵盖改期、追加链接、重复请求、轮换 Feed 后原 UID 不变；相同重建保持字节/ETag/Last-Modified；304、HEAD。
- 涵盖持久 block、用户间链接隔离、版本冲突、导入确认绑定、导出无令牌、遗漏偏好合并、取消关注的历史保留、显式排除、事务 outbox 回滚、时间待定全天事件、来源缺页保留旧赛程、身份稳定、Origin/Host/游客边界与非法链接。
- 本机浏览器：游客选择湖人 → 本地体验登录后选择保留 → 保存 → 订阅页显示 12 场已发布演示比赛。测试身份明确为本机体验账号。
- Jolpica HTTP 样本返回 23 个大奖赛，完整数量相符；写入 115 个非 demo 分场次。第三方赛程样本，未独立核验所有时间、授权范围或取消语义。
- Web TypeScript 与 production build 通过；npm audit 0 vulnerabilities（构建时锁定版本）。
- 初始迁移在独立 SQLite 校验库执行，`alembic check` 无差异；MySQL 离线 DDL 生成成功，不等于 Azure MySQL 已运行。

## 尚未通过的外部验收

| 项目 | 状态 | 精确缺口 |
| --- | --- | --- |
| T01 系统日历变更实验 | 待实测 | 公网 HTTPS feed；Apple/Google 真实订阅、刷新与手机观察 |
| T02 手机链接行为 | 待实测 | 目标 App、日历客户端与账号权益组合 |
| T03 数据源权限覆盖 | 部分样本 | NBA/足球 key、许可/配额/全季与取消语义；F1 对照官方 |
| T04 YouTube 通知 | 本地协议测试通过，真实 Hub 待验 | 独立 key、公网回调、真实租约续订与补查观测 |
| T05 双仓 CI | 本地通过、远端未运行 | workflows 仅检查，不含部署；尚未 push |
| T06 数据模型 | 本地部分通过 | MySQL staging 迁移、并发和压测、完整身份映射 |
| Firebase 登录 | 未验证 | 独立 Firebase 项目与回调域 |
| Azure 消费/调度 | 未验证 | 独立目标环境及部署配置 |

测试假链接只进入隔离内存库或自动清理的 UI 临时库，不生成真实比赛的官方观看入口。私人 Feed 地址、会话和数据库凭据均不记录于本文件。


## 创作者链路新增证据

- 新增 17 项隔离测试：重复匹配保持 Feed 版本/ETag；标题冲突撤下自动链接；block 与用户隔离；暂停保留、删除预览与 pin；人工确认/忽略及版本冲突；频道伪造、视频不可用；分页中途不能按缺失删除；28 天元数据清理；签名/主题/XML/体积限制、通知去重；租约 80% 续订；验证意图先提交再发 Hub 请求；多用户共享调度。
- `ruff check .` 与 33 项 pytest 通过，仍有 2 项原有第三方弃用提醒。测试使用合成数据，不代表真实匹配准确率或 YouTube 通知实测。
- 新迁移 `b5e150717423` 在独立 SQLite 校验库 upgrade/check 通过；MySQL 全量离线 DDL 通过。随后对本地体验库 SQLite backup，核对初始表结构，stamp 初始迁移并 upgrade。所有原有 11 张表逐行一致，新增 3 张表。保留了 163 条比赛与原账号/Feed；备份位于忽略目录，权限 0600。
- 主预览浏览器：恢复已有本地账号，查找频道显示未配置 YouTube 的错误；未添加合成频道到主预览数据。
- `experiments.creator_ui` 使用独立临时库和显式合成频道/视频；反向代理客户端 production build。浏览器完成频道身份预览 → NBA 范围 → 确认关注 → 1 个待确认视频/1 个自动关联 → 人工确认 → 同一比赛显示 2 链接 → 暂停 → 删除预览移除 1、保留 1 → 删除后比赛只剩人工确认的固定链接。
- 1440×1000 检查空状态/错误；1280×800 检查频道与审核卡片；1024×800 检查偏好编辑器和删除操作，DOM scrollWidth = viewportWidth = 1024，无横向溢出。该浏览器证据只针对本地构建和合成输入。
- 隔离 UI 服务和临时生产预览在验证后停止，临时库自动清理；用户的 Web/API/worker 继续运行。

仍需：至少 200 条真实标注视频集及自动关联准确率、真实 Hub/续订/补查、API 配额与完整频道覆盖、Azure MySQL 并发/租约恢复、系统日历和手机显示测试。当前没有达到上述外部验收。

## 应用授权与 MCP 新增证据

- 12 项新增测试使用隔离 SQL 数据、真实官方 SDK 的授权 HTTP handlers 与 Streamable HTTP client。当前共45项通过；ruff通过，保留2项第三方弃用提醒。
- 覆盖 PKCE 校验/单次兑换、resource/issuer/expiry、刷新轮换/重放撤销、public client 撤销、不同用户隔离、越权 scopes、拒绝授权、账号删除和过期清理；scope 不允许扩展再次授权其他客户端。
- MCP 与 HTTP 同键链接写入返回相同结果，改参数报冲突；失败不写回执、outbox 与配置同事务；block 后手动重试也不会恢复链接；导出无令牌，导入绑定预览和版本；查询分页绑定条件和更新时间。
- 前端 OpenAPI 类型更新，typecheck/production build通过。`/connect` 与设置的连接管理均在本机浏览器实际操作。1440×1000检查授权内容与按钮；1280×800、1024×800 DOM无横向溢出。
- `scripts.check_oauth_locally` 通过真实loopback网络和浏览器完成本地账号登录→权限确认→PKCE兑换→官方SDK `get_my_calendar`→设置撤销→原令牌401。`feed:read` 保持未选；没有打印令牌、授权码或私人Feed地址，没有写入原关注/比赛。临时18788进程和两个验收标签已关闭，测试连接已撤销。
- `721f5dc7b8c2` 在独立SQLite upgrade/check通过，MySQL离线DDL通过；本机API/worker停止后通过SQLite backup生成0600备份，再迁移。原有14表逐行相同，163场事件保留。API/worker重新启动成功，原Web显示48条合成赛程和湖人关注。
- 这不代表真实Firebase登录、Azure ASGI/MySQL并发、目标Codex客户端、Chrome安装与后台生命周期、生产限流或Client ID Metadata Documents已验收。T26/T28/T29仍为in_progress；下一批完成Chrome客户端并继续外部能力准备。
