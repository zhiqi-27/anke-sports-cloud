# 本地证据 · 2026-09-09

产品：Anke Sports。范围：本机 SQLite/API/worker/浏览器。无 production、staging、手机或真实 Firebase 结论。

- `uv run pytest -q`：16 passed，0 failed；2 项第三方库弃用提醒。
- 涵盖改期、追加链接、重复请求、轮换 Feed 后原 UID 不变；相同重建保持字节/ETag/Last-Modified；304、HEAD。
- 涵盖持久 block、用户间链接隔离、版本冲突、导入确认绑定、导出无令牌、遗漏偏好合并、取消关注的历史保留、显式排除、事务 outbox 回滚、时间待定全天事件、来源缺页保留旧赛程、身份稳定、Origin/Host/游客边界与非法链接。
- 本机浏览器：游客选择湖人 → 本地体验登录后选择保留 → 保存 → 订阅页显示 12 场已发布演示比赛。测试身份明确为本机体验账号。
- Jolpica HTTP 样本返回 23 个大奖赛，完整数量相符；写入 115 个非 demo 分场次。第三方赛程样本，未独立核验所有时间、授权范围或取消语义。
- Web TypeScript 与 production build 通过；npm audit 0 vulnerabilities（构建时锁定版本）。
- 初始迁移在独立 SQLite 校验库执行，`alembic check` 无差异；MySQL 离线 DDL 生成成功，不等于 Azure MySQL 已运行。

## 尚未通过的外部验收

| 项目 | 状态 | 精确缺口 |
| --- | --- | --- |
| T01 系统日历变更实验 | 待实测 | 公网 HTTPS feed；Apple/Google 真实订阅、刷新与手机观察 |
| T02 手机链接行为 | 待实测 | 目标 App、日历客户端与账号权益组合 |
| T03 数据源权限覆盖 | 部分样本 | NBA/足球 key、许可/配额/全季与取消语义；F1 对照官方 |
| T04 YouTube 通知 | 未完成 | 独立 key、公网回调、租约续订与补查实现 |
| T05 双仓 CI | 本地通过、远端未运行 | workflows 仅检查，不含部署；尚未 push |
| T06 数据模型 | 本地部分通过 | MySQL staging 迁移、并发和压测、完整身份映射 |
| Firebase 登录 | 未验证 | 独立 Firebase 项目与回调域 |
| Azure 消费/调度 | 未验证 | 独立目标环境及部署配置 |

测试假链接只进入隔离内存库，不生成真实比赛的官方观看入口。私人 Feed 地址、会话和数据库凭据均不记录于本文件。
