# 本机 MySQL 验证 · 2026-09-10

实际执行 MySQL Community 8.4.11 macOS ARM64 进程；官方归档 MD5 为 `6e89113f04f2af85d0a164573493db3a`，下载后相同。仅 Unix socket、关闭 TCP 与 mysqlx，独立 0700 临时目录，无系统安装或自启服务。

首次运行发现并修复：默认排序规则合并大小写/重音不同身份；默认事务快照导致两个旧确认用例失败；并行首次创建预算行发生 InnoDB 1213 死锁。另将跨 worker 测试观察者切换为新事务，以读取已提交结果。没有删掉失败用例或放宽业务断言。

最终结果：

- MySQL 模式完整测试：179 passed，2 条既有弃用警告，27.21 秒（session65425）。数据库 fixtures 使用真实 MySQL；纯逻辑测试及明确测试 SQLite 迁移的用例保持原对象。
- 默认 SQLite：177 passed、2 skipped（MySQL 专用），2 条既有警告，9.76 秒（session67077）。
- ruff 通过；OpenAPI/config schema 导出后字节无变化。
- 旧库迁移、回退、再升级和模型 check 通过；25 张业务表精确比较，大小写/重音/尾空格身份保持分离。
- 并发预算预留、账号删除竞争、频道租约、旧匹配确认、关注与发布等既有用例均通过 MySQL 模式。

CI 添加固定 MySQL 8.4.11 镜像；未推送或远程执行。尚未证明 Azure 资源、TLS、网络、云触发器、真实数据并发负载或设备日历行为。恢复与复现步骤见 `docs/mysql-validation.md`。
