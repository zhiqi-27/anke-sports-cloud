# 2026-09-10 · 账号删除与竞态验收

本批为 T07/T31/T33 的本地实现证据，任务保持 in_progress。无云资源创建、push、部署或真实 Firebase 删除。

## 修复与测试

独立 SQLite 连接先复现“请求读到旧账号，删除完成后仍保存个人配置”的失败用例。修复后删除与写入/授权/发布按账号顺序提交，旧请求不能恢复配置、回执或个人任务。

新增 `tests/test_account_deletion.py` 13 项通过，完整后端 **154 passed**，ruff、差异格式检查通过。仍有两条既有 Starlette/AnyIO 弃用警告。覆盖：

- 真实 HTTP 删除、确认拒绝、撤销旧 Feed/本地会话/扩展令牌与刷新凭据，其他账号与公共比赛保留。
- 旧配置、幂等回执与任务入队不能复活删除账号；事务错误回滚；旧 Worker 返回不能再次发布。
- 授权码及刷新交换在读取 owner 后暂停，另一连接删除，再恢复真实 HTTP 请求，返回 invalid_grant 且不生成凭据。
- 最后一位所有者删除后，已排队频道/视频/作者资料任务不再联网；另一所有者仍需要时继续处理。
- 合成 Firebase 验证适配器：固定目标、错误目标不调用、失败重试与重放、远端已不存在的幂等；实际 Firebase 调用为 0。
- 包含合成私人 Feed 地址的异常只返回统一 503，应用日志与正文无该哨兵值。

曾尝试在频道网络阶段持有用户写锁，导致既有租约恢复测试失败；已撤回该策略，联网前检查兴趣，个人提交阶段锁定。相关频道/OAuth/日历回归及最终全量套件通过。没有用弱化测试方式处理失败。

## 网页验收

客户端使用最终 `npm run build` 产物、独立 IPv6 临时 SQLite 夹具，明确显示合成账号。Web/扩展 typecheck 与 production build 通过，OpenAPI 和生成的客户端契约已更新。

实际浏览器按钮→确认弹窗→键盘取消，全部脱敏夹具字段与之前相同；焦点回到删除按钮。再次打开后 Tab/Enter 确认，页面完整跳转到游客日历，显示“账号数据已删除。请在系统日历中移除旧订阅。”确认弹窗在 1440×1000、1280×800、1024×800 均无页面或弹窗横向溢出，实际渲染已检查。删除提示在三种宽度可读，console error=0。

删除后读回：账号 tombstone/config 空、Feed 撤销/正文与密文清空，个人投影/链接/会话/任务为 0，另一合成账号保留，未登录个人日历 401。网页夹具原先投影/私人链接为 0，它不单独证明非空数据删除；非空清理由 HTTP 测试证明。见 [之前](privacy-browser-before-2026-09-10.json)、[之后](privacy-browser-after-2026-09-10.json)。夹具已停止、临时库正常清理，浏览器 20 已关闭。

## 主服务与状态

无数据库迁移。停旧 API/Worker 后作 0600 SQLite 备份 `data/before-privacy-20260909-200145.db`（文件名 UTC，工作区日期 9 月 10 日）。重启最终代码，真实 HTTP 登录/个人日历通过，只注销脚本自己的临时 cookie。全部 **25 表逐行哈希一致**，163 场、原账号配置/关注/Feed 和会话均保留。见 [主服务读回](privacy-main-readback-2026-09-10.json)。

期间现有自动调度在本地时间 03:43 完成了一次真实 Jolpica 获取（last_success=2026-09-09T19:43:01.840469+00:00），这是原已启用来源的定时任务，不是合成删除测试。重启未再次提前抓取。不能把本批全部活动描述为“没有真实上游请求”。

主设置页实际显示本地体验账号、原湖人关注及删除按钮禁用。临时设置页 21 检查后关闭；原待确认关注草稿 16、游客公共订阅 12、旧合成维护 10 保留。

## 未验收

真实 Firebase 删除/退出、多端账号变化、MySQL 并发、Azure Queue 与日志/告警、设备缓存清除、备份恢复防止删除账号复活。旧请求已经发出的数据不能远程收回。新增个人任务锁对大规模通知/发布积压的影响，仍应与 1,000 用户通知到 Feed 的完整容量验收一起验证；之前查询容量报告不是这项证明。
