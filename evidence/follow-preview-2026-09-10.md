# 关注预览 · 2026-09-10

范围：T11 / T23，补充 QA-01、QA-06、QA-13 的本地证据。不是云端、真实账号或手机验收。

## 代码验证

- 后端 `uv run pytest -q`：98通过（新增6项）；`uv run ruff check .`通过。保留既有2项Starlette/httpx/anyio弃用警告。
- Web `npm run typecheck`、`npm run extension:typecheck`、最终 `npm run build`通过；两仓 `git diff --check`通过。未因本批业务/UI变更重复运行无关扩展行为测试。
- OpenAPI导出及客户端生成类型同步。无需数据库迁移。

## 实际浏览器与 HTTP

使用独立临时SQLite、合成赛程与账号，API运行在3003，前端使用3002的production构建，后台运行真实共享任务处理器。

1. 原关注湖人和勇士，Feed 16条。取消湖人后预览：新增0、移除未来6、其他关注保留2、历史保留2、结果10。Escape关闭，焦点回到“预览变更”；HTTP读回revision仍为1、两个关注均保留。
2. 键盘Enter重开、Tab可到达确认按钮。1440×1000、1280×800、1024×800浏览器截图与DOM几何检查无页面/弹窗横向溢出。800高视口中弹窗可滚动，键盘焦点会带出底部按钮。
3. 预览后在临时库修改待移除比赛标题并原子入队发布，浏览器提交收到过期预览错误，配置不变。修复底部提交后顶部错误不可见的问题：错误获得焦点并滚入视口。1024宽复测中alert顶部为158.5px，提示可见；选择“重新预览”读取新标题。
4. 浏览器“确认保存”成功，侧栏仅保留勇士，显示“已保存，订阅源更新中”。随后真实HTTP读取：revision 2，10条有效比赛、6条原UID的CANCELLED移除通知，全部16个UID集合保持一致，Feed状态为published。机器记录见 [JSON](follow-preview-2026-09-10.json)，不含私人Feed地址/令牌。
5. 夹具页面无脚本console error。验收后关闭夹具标签15并停止3003，临时库由退出流程删除；`experiments/follows_ui.py`可重建。初次打开服务时尚未监听，出现连接失败页；没有将其计为成功页面。

## 主预览读回

主API/worker重启至本批代码。主库users、feeds、events、projections、sources五表完整行哈希在重启及浏览器预览前后相同，163条比赛保留。匿名关注预览返回401。

主Web `http://127.0.0.1:3000/following`（标签16）读取原有体验账号。保留湖人并新增NBA草稿时，预览新增8（其中历史3）、结果20；尚未确认保存，主库关注不变。为用户保留该预览与已有日历、公共订阅和维护页面。

## 边界

本次验证了本地API、真实浏览器操作、任务发布和HTTP ICS内容。源发布、外部日历抓取、手机显示与播放是不同证据。真实Firebase多端、MySQL并发、外部客户端移除/刷新、规模与云发布未完成，T11/T23继续标记in_progress。确认摘要是当前影响检查，不锁定持续更新的整个赛程库。
